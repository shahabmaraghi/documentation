import { BaseUrlCard } from "./components/BaseUrlCard";

# نمای کلی مستندات

به مستندات وب‌سرویس پیام کوتاه قاصدک خوش آمدید. این مستندات شامل راهنمای کامل استفاده از API های مختلف برای ارسال پیامک، مدیریت OTP، دریافت گزارش‌ها و سایر قابلیت‌های سرویس پیام کوتاه قاصدک می‌باشد.

## آدرس پایه‌ی API

برای ارسال هر درخواست باید آن را بر اساس آدرس پایه‌ی اصلی سرویس بسازید. تمام متدهای REST موجود در این مستندات از این آدرس آغاز می‌شوند و سپس ادامه‌ی مسیر مربوط به متد مورد نظر اضافه می‌شود.

<BaseUrlCard url="https://gateway.ghasedak.me/rest/api/v1/" />

## بخش‌های اصلی

### 📤 وب سرویس ارسال

این بخش شامل متدهای مختلف برای ارسال پیامک است:

- **[ارسال تکی](/guides/send-single)**: برای ارسال پیامک به یک شماره گیرنده
- **[ارسال گروهی](/guides/send-bulk)**: برای ارسال یک پیام به چندین گیرنده مختلف
- **[ارسال گروهی نظیر به نظیر](/guides/send-bulk-peer-to-peer)**: برای ارسال پیامک‌های مختلف به گیرندگان مختلف

### 🔐 سرویس اعتبار سنجی

مدیریت کامل پیامک‌های OTP و قالب‌های اعتبارسنجی:

- **[ارسال پیامک اعتبار سنجی (OTP)](/guides/sendOtpSms)**: ارسال پیامک OTP با استفاده از قالب‌های از پیش تعریف شده
- **[ارسال پیامک OTP جدید](/guides/send-otp-new)**: ارسال OTP با قابلیت‌های پیشرفته‌تر
- **[دریافت پارامترهای قالب OTP](/guides/otp-template-params)**: دریافت اطلاعات و پارامترهای قالب‌های OTP

### 📊 گزارش وضعیت

مشاهده وضعیت و جزئیات پیام‌های ارسال شده:

- **[وضعیت پیام های ارسالی](/reports/outbox-status)**: بررسی وضعیت ارسال پیامک‌ها

### 📥 پیام های دریافتی

گزارش پیام‌های ورودی خطوط شما:

- **[100 پیام آخر](/inbox/latest-100)**: مشاهده آخرین پیام‌های دریافتی
- **[صفحه بندی](/inbox/paginated)**: دریافت پیام‌های دریافتی به صورت صفحه‌بندی شده

### 📚 کتابخانه‌ها و SDK

برای راحتی و سرعت بیشتر در توسعه، کتابخانه‌های آماده برای زبان‌های برنامه‌نویسی مختلف فراهم کرده‌ایم:

- **[SDK](/guides/sdk)**: کتابخانه‌های آماده برای PHP، Node.js، Python، C#، .NET Core، Java و Go

با استفاده از این کتابخانه‌ها می‌توانید به راحتی و بدون نیاز به نوشتن کدهای پیچیده، با API قاصدک ارتباط برقرار کنید. هر کتابخانه شامل:

- نصب آسان از طریق Package Manager های استاندارد
- مستندات کامل و نمونه کدهای کاربردی
- پشتیبانی از تمام متدهای API
- مدیریت خودکار خطاها و استثناها

## شروع کار

برای شروع استفاده از API های قاصدک:

1. **دریافت API Key**: ابتدا باید کلید API خود را از پنل مدیریت قاصدک دریافت کنید
2. **انتخاب متد مناسب**: بر اساس نیاز خود، یکی از متدهای ارسال را انتخاب کنید
3. **مطالعه مستندات**: مستندات هر متد شامل جزئیات کامل پارامترها و نمونه کدهای مختلف است
4. **تست درخواست**: می‌توانید از بخش تست درخواست در هر صفحه برای آزمایش API استفاده کنید

## نکات مهم

- تمام درخواست‌ها باید از طریق HTTPS ارسال شوند
- برای احراز هویت، API Key خود را در هدر درخواست قرار دهید
- در صورت بروز خطا، می‌توانید از [جدول خطاها](/error-table) برای بررسی کدهای خطا استفاده کنید
- نمونه کدهای مختلف برای زبان‌های برنامه‌نویسی مختلف در هر صفحه موجود است

## پشتیبانی

در صورت بروز مشکل یا نیاز به راهنمایی بیشتر، می‌توانید با تیم پشتیبانی قاصدک تماس بگیرید.

