import { ScalarApiReference } from "@/app/components/ScalarApiReference";
import { OperationBox } from "@/app/components/OperationBox";
import { paginatedApiSpec } from "./api-spec";

# صفحه بندی پیام های دریافتی

<div className="operation-hero">
  <div className="operation-hero__text">
    <p>از این متد برای گرفتن لیست پیام های دریافتی به صورت صفحه بندی استفاده می شود. در این متد حداکثر 200 پیام بازگردانده می شود.</p>
  </div>

  <OperationBox
    method="GET"
    endpoint="https://gateway.ghasedak.me/rest/api/v1/WebService/GetReceivedSmsesPaging"
  />
</div>

<ScalarApiReference spec={paginatedApiSpec} />

## جدول خطاها

چنانچه درخواست‌های ارسالی شما با خطای خاصی مواجه شد، برای آگاهی از دلایل آن می‌توانید از [جدول خطاها](/error-table) کمک بگیرید.

